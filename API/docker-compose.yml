version: "3.8"

services:
  airflow-apiserver:
    build:
      context: .
      dockerfile: Dockerfile.airflow
    volumes:
      - ./src/dags:/opt/airflow/dags
      - ./dist:/opt/airflow/dist
      - ./package.json:/opt/airflow/package.json
      - airflow_db:/opt/airflow
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
    command: api-server
    ports:
      - "8080:8080"

  airflow-scheduler:
    build:
      context: .
      dockerfile: Dockerfile.airflow
    volumes:
      - ./src/dags:/opt/airflow/dags
      - ./dist:/opt/airflow/dist
      - ./package.json:/opt/airflow/package.json
      - airflow_db:/opt/airflow
    environment:
      - AIRFLOW__CORE__EXECUTOR=LocalExecutor
      - AIRFLOW__CORE__LOAD_EXAMPLES=False
    command: scheduler

  lb4-api:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./dist:/usr/src/app/dist
      - ./package.json:/usr/src/app/package.json
    ports:
      - "3000:3000"
    command: xvfb-run --server-args="-screen 0 1280x720x24" node dist/scripts/run-main.js

volumes:
  airflow_db:
